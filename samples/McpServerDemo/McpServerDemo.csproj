<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<OutputType>Exe</OutputType>
		<TargetFramework>net10.0</TargetFramework>
		<RootNamespace>McpServerDemo</RootNamespace>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<ItemGroup>
		<!-- C# MCP SDK (modelcontextprotocol/csharp-sdk).
		     ModelContextProtocol = Core + Hosting/DI (stdio), the full set. -->
		<PackageReference Include="ModelContextProtocol" Version="1.2.0" />
		<PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.0" />
	</ItemGroup>

	<!-- Reference the dotcl runtime (Library). The Reflection.Emit-based
	     compiler/runtime runs as-is on CoreCLR. -->
	<ItemGroup>
		<ProjectReference Include="..\..\runtime\DotCL.Runtime.csproj" />
	</ItemGroup>

	<!-- Bundle dotcl.core next to the exe so DotclHost.FindCore() picks it
	     up at startup. -->
	<ItemGroup>
		<None Include="..\..\compiler\dotcl.core" Link="dotcl.core"
		      Condition="Exists('..\..\compiler\dotcl.core')">
			<CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
		</None>
	</ItemGroup>

	<!-- Bundle contrib/ (for any (require :dotnet-class) etc.). -->
	<ItemGroup>
		<None Include="..\..\contrib\**\*.lisp" LinkBase="contrib">
			<CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
		</None>
	</ItemGroup>

</Project>
