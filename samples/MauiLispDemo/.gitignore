# Per-machine task runner config (paths, scrcpy flags etc.)
.env

# MAUI / .NET build artifacts (the root .gitignore already covers bin/ obj/,
# but list them here for the sample too)
bin/
obj/

# Diagnostic log written by MauiProgram (output dir-relative on Windows,
# AppDataDirectory on Android — grep here targets the Windows output)
dotcl-maui.log
